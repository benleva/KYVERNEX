const promptEl=document.querySelector('#prompt');const validateBtn=document.querySelector('#validate');const scoreEl=document.querySelector('#score');const statusEl=document.querySelector('#status');const contextCheck=document.querySelector('#contextCheck');const contractCheck=document.querySelector('#contractCheck');const traceCheck=document.querySelector('#traceCheck');const inferenceCheck=document.querySelector('#inferenceCheck');const actsEl=document.querySelector('#acts');const traceEl=document.querySelector('#trace');const implicit=/\b(procedi|come prima|quello precedente|ultima versione|stesse regole)\b/i;const adversarial=/\b(inventa|ignora i dati|fabbrica|senza prove)\b/i;const patterns=[['READ',/\b(leggi|analizza|controlla|review|read|analy[sz]e)\b/i],['CLASSIFY',/\b(analizza|classifica|separa|analy[sz]e|classify)\b/i],['SUMMARIZE',/\b(riassumi|sintetizza|summary|summari[sz]e)\b/i],['VERIFY',/\b(verifica|controlla|prove|verify|evidence)\b/i],['COMPARE',/\b(confronta|compare|differenze)\b/i],['EXTRACT',/\b(estrai|extract|elenca)\b/i],['GENERATE',/\b(crea|scrivi|genera|inventa|write|generate)\b/i],['TRANSLATE',/\b(traduci|translate)\b/i]];function analyze(text){const clean=text.trim();const acts=patterns.filter(([,r])=>r.test(clean)).map(([name])=>name);const hasImplicit=implicit.test(clean);const isAdversarial=adversarial.test(clean);const mentionsObject=/\b(pdf|documento|file|testo|articolo|contratto|report)\b/i.test(clean);let status='VALID';let css='good';let score=96;let context='NOT REQUIRED';let contract='VALID';let unsupported='0';const trace=[];if(!clean){status='INVALID INPUT';css='bad';score=0;context='MISSING';contract='INVALID';trace.push('Input assente: pipeline interrotta.')}else{trace.push('Input preservato e normalizzato.');if(hasImplicit){context='MISSING';contract='PARTIAL';status='CLARIFICATION REQUIRED';css='warn';score=58;trace.push('Riferimento implicito rilevato.');trace.push('Nessuna fonte canonica collegata nel browser.');trace.push('Richiesta di chiarimento emessa.')}else{context='NOT REQUIRED';trace.push('Nessun riferimento implicito necessario.')}if(mentionsObject){trace.push('Oggetto documentale rilevato come REFERENCED.');if(!/\b(incolla|seguente|qui sotto)\b/i.test(clean)){contract=contract==='PARTIAL'?'PARTIAL':'PARTIAL';status=status==='CLARIFICATION REQUIRED'?status:'OBJECT REQUIRED';css='warn';score=Math.min(score,72);trace.push('Il contenuto dell’oggetto non è disponibile nella demo.')}}if(isAdversarial){status='REJECTED';css='bad';score=18;contract='REFUSED';unsupported='BLOCKED';trace.push('Richiesta di conclusione non supportata rilevata.');trace.push('Il contratto rifiuta la fabbricazione di evidenze.')}if(!acts.length&&!hasImplicit&&!isAdversarial){contract='PARTIAL';status='COGNITIVE ACT UNKNOWN';css='warn';score=45;trace.push('Atto cognitivo non classificato.')}}return{acts:acts.length?acts:['QUESTION'],status,css,score,context,contract,unsupported,trace}}function render(){const result=analyze(promptEl.value);scoreEl.textContent=`${result.score}/100`;statusEl.textContent=result.status;statusEl.className=`status ${result.css}`;contextCheck.textContent=result.context;contractCheck.textContent=result.contract;traceCheck.textContent=result.trace.length?'VISIBLE':'MISSING';inferenceCheck.textContent=result.unsupported;actsEl.textContent=result.acts.join(' → ');traceEl.innerHTML='';result.trace.forEach(item=>{const li=document.createElement('li');li.textContent=item;traceEl.appendChild(li)})}validateBtn.addEventListener('click',render);document.querySelectorAll('.example').forEach(button=>button.addEventListener('click',()=>{promptEl.value=button.dataset.text;render()}));document.querySelector('#copyCommand').addEventListener('click',async event=>{const command='git clone https://github.com/benleva/ARGUS.git\ncd ARGUS/engine-reference\npython -m pip install -e ".[test]"\nargus benchmark --corpus ../validation/corpus/pvc-0001-0050.json --output-dir ../validation/reports/local';try{await navigator.clipboard.writeText(command);event.currentTarget.textContent='COPIATO';setTimeout(()=>event.currentTarget.textContent='COPIA',1600)}catch{event.currentTarget.textContent='SELEZIONA'}});render();
